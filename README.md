# cpp-search-server
A lightweight search engine with a ranking system written in C++.

Solution:
- Relevance is calculated using the TF-IDF formula.
- The architecture is implemented in FP(functional programming style), maintaining readability and scalability.

Result:
The solution indexes documents, efficiently parses queries, calculates relevance, and returns results based on term frequency and match accuracy.
